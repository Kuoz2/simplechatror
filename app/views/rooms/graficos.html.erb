<%= turbo_frame_tag "rooms_controller" do %>
  <div class="flex h-screen">
    <!-- Sidebar -->
    <div class="w-60 bg-[#181D31] border border-sky-500 h-screen overflow-y-auto">
      <%= render 'shared/side_bar' %>
    </div>

    <!-- Gráfico -->
    <div class="flex-1 p-6 bg-gray-800">
      <%= render 'graficas' %>
    </div>
  </div>
<% end %>