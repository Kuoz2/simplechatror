<%= turbo_stream_from 'users_rooms_channel'%>
<%= turbo_stream_from 'room_details_channel'%>
<nav class="bg-[#181D31] w-60 flex flex-col items-center justify-between border border-sky-500 h-screen overflow-y-auto">
   <div class="flex-1 overflow-y-auto w-full">
    <%= render 'shared/user_rooms' %>
  </div>
  <!-- Formulario fijo en la parte inferior -->
  <div class="w-full">
    <%= render 'shared/create_room' %>
  </div>
</nav>
